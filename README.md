# get_certificate
Tool for get cetificate site/server

## use
Use in CMD ```cert s``` for setup virtual environment and install dependencies
Use in CMD ```cert c``` for consult the certificate of a host